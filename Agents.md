# ProjectWI 프로젝트 규칙

## 프로젝트 기준

- 프로젝트 이름은 `ProjectWI`입니다.
- Unity 6000.3 계열로 제작합니다.
- 장르는 2D 판타지 영토 확장 전략 게임입니다.
- 핵심 플레이는 60개 성의 점령, 성 중심 내정, 영웅 배치, 시설 건설, 세력 전쟁, 턴 진행입니다.
- 일반 병사 부대는 사용하지 않으며 영웅의 배치와 조합이 군사력입니다.
- 이전 방치형 던전 게임의 코드, 데이터, UI, 명칭을 다시 도입하지 않습니다.
- 현재 기획 기준은 `GameDocuments/GameDesign.md`입니다.

## 작업 및 기록

- 사용자와 한국어로 소통합니다.
- 작업 시작 전 UnityMCP 연결과 `GameDocuments/ProjectStatus.md`를 확인합니다.
- 게임내에 변경이 필요한 경우 MCP 연결이 되어 있지 않다면, 우선 작업을 실행하지 말고 MCP 연결을 해야합니다. 저와 의논 하며 MCP 연결을 최우선으로 진행.
- 하이어라키, 컴포넌트, 프리팹, ScriptableObject 작업은 가능한 한 UnityMCP를 사용합니다.
- 작업 완료 시 `ProjectStatus.md`, `DataManual.md`, `DataDocument.md`를 현재 구현에 맞게 갱신합니다.
- 씬에는 Main Camera와 주 조명을 유지합니다.
- 재사용 UI와 게임 오브젝트는 프리팹으로 구성합니다.

## 코드 규칙

- 클래스 이름은 `WI` 접두사를 사용합니다.
- Unity C# 코딩 스타일을 따릅니다.
- `if`, `for`, `while`에는 중괄호와 줄바꿈을 사용합니다.
- 상태 비교는 의도가 명확하도록 `true`, `false`를 명시합니다.
- 멤버 변수와 함수의 역할은 한국어 주석으로 기록합니다.
- 하드코딩된 성, 영웅, 시설, 세력 밸런스 데이터 대신 ScriptableObject 데이터를 사용합니다.
- 에셋 생성을 위한 일회성 로직을 런타임 코드에 넣지 않습니다.
- 스크립트 변경 후 Unity 컴파일과 콘솔 오류를 확인합니다.
- 데이터나 UI 등을 런타임에 동적 생성을 해서는 안됩니다. 데이터나 UI를 만들어달라고 하면 런타임이 아닐때도 볼 수 있어야 합니다.
- UI와 코드는 기능 단위로 적당히 분리합니다. 불필요하게 복잡한 구조를 만들지는 않되, 서로 다른 화면과 책임을 하나의 거대한 UXML이나 클래스에 계속 추가하지 않습니다.
- 큰 UI는 조립용 루트 UXML과 화면별 Template UXML로 나누고, 큰 컨트롤러는 동일 클래스의 기능별 partial 또는 별도 책임 클래스로 나누어 검토하기 쉽게 유지합니다.

## 데이터 및 UI 규칙

- 전략 게임의 마스터 데이터는 ScriptableObject에서 편집 가능해야 합니다.
- UI는 성을 선택해 내정, 영웅 배치, 건설, 선술집, 출정을 처리하는 Castle-Centric Flow를 유지합니다.
- 화면에 표시되는 고정 문구는 데이터베이스의 문자열 UID를 통해 관리합니다.
- 이미지가 준비되지 않은 곳은 교체 가능한 빈 이미지 슬롯으로 둡니다.
- PC와 모바일 입력을 모두 고려하며 핵심 버튼의 터치 영역을 충분히 확보합니다.

## 파일 규칙

- 텍스트 파일은 UTF-8 without BOM으로 저장합니다.
- 줄바꿈은 CRLF를 사용합니다.
- 사용자가 요청하지 않은 커밋이나 푸시는 수행하지 않습니다.
